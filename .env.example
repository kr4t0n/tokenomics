# Cursor session token (optional — auto-extracted from local Cursor DB if omitted)
# To find manually: open https://cursor.com/settings in browser, open DevTools Network
# tab, look for requests to /api/usage, copy the WorkosCursorSessionToken cookie value.
CURSOR_SESSION_TOKEN=

# Server port
PORT=3000
